<br>
<p>
</p>
<br>
<div class="home_view_username">
<span>
  <% if @is_current_user %>
    Welcome back, <%= @user.username %>!
  <% else %>
    Home page of <%= @user.username %>
  <% end %>
</span>
  </div>
<br><br><br><br>
  <table class="user_show_table">
    <tr>
      <td class="user_home_show_td"> <%= link_to('Following', show_followeds_url(@user.id), :method => :get) %> </td>
      <td class="user_home_show_td"> <%= link_to('Followers', show_followers_url(@user.id), :method => :get) %> </td>
      <td class="user_home_show_td"> <%= link_to('Tweets', show_tweets_url(@user.id), :method => :get) %> </td>
    </tr>
    <tr>
      <td class="user_home_show_td"> <%= @user.num_followeds %> </td>
      <td class="user_home_show_td"> <%= @user.num_followers %> </td>
      <td class="user_home_show_td"> <%= @user.num_tweets %> </td>
    </tr>
  </table>
